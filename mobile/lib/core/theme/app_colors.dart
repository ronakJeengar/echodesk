import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Dark Canvas & Surfaces
  static const Color background = Color(0xFF090D16);
  static const Color surface = Color(0xFF111726);
  static const Color surfaceElevated = Color(0xFF172033);
  static const Color border = Color(0xFF202B42);
  static const Color borderSubtle = Color(0xFF1B2438);

  // AI & Brand Accents
  static const Color primary = Color(0xFF06B6D4); // Cyan / AI pulse
  static const Color primaryLight = Color(0xFF22D3EE);
  static const Color primaryDark = Color(0xFF0891B2);
  static const Color primaryGlow = Color(0x3306B6D4);

  static const Color secondary = Color(0xFF8B5CF6); // Violet
  static const Color secondaryGlow = Color(0x338B5CF6);

  // Semantic Status Colors
  static const Color success = Color(0xFF10B981); // Emerald
  static const Color warning = Color(0xFFF59E0B); // Amber
  static const Color danger = Color(0xFFEF4444);  // Rose
  static const Color recordingRed = Color(0xFFE11D48);

  // Text Hierarchy
  static const Color textPrimary = Color(0xFFF8FAFC);
  static const Color textSecondary = Color(0xFF94A3B8);
  static const Color textMuted = Color(0xFF64748B);
  static const Color textDisabled = Color(0xFF475569);
}
